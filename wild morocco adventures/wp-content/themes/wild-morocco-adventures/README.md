# Wild Morocco Adventures theme

Install with the required WMA Core plugin. The theme contains no page builder and uses PHP templates, Gutenberg content, custom CSS and minimal vanilla JavaScript. Client business data belongs in Wild Morocco > Business settings; tour data belongs in WMA Core.

Fallback images in `assets/images` are original AI-generated project assets documented in `docs/ASSET-LICENSES.md`.
